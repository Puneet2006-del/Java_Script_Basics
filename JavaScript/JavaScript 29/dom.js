// DOM = Document Object Model It is a programming interface for web documents. When a web page is loaded, the browser turns the HTML into a DOM — a tree-like structure where each element becomes a node (like branches and leaves).


    
document.getElementsByClassName('heading').innerHTML = <h1>"Hello world</h1>